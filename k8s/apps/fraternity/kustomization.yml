---
kind: Kustomization
namespace: fraternity

resources:
  - namespace.yml
  - frat-logs
  - frat-shop
  - fraternity-api

configMapGenerator:
  - name: common-config
    literals:
      - DB_HOST=infra-main-cluster.postgres.svc.cluster.local
